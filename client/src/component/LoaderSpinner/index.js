import Loader from 'react-loader-spinner'

const LoaderSpinner = () => {
    return (
        <Loader
            type="Oval"
            color="black"
            height={20}
            width={20}
        />
    )
}

export default LoaderSpinner